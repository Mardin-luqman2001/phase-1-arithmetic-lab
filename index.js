
// Write your code here

// Define the variables num1 and num2
const num1 = 31;
const num2 = 2;

// Create the multiply variable and set it to the equation
const multiply = num1 * num2;

// Log the result
console.log(multiply); // Output: 62
// Create a variable called random that generates a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1; // Generates a random integer between 1 and 100

// Create variables num3 and num4
const num3 = 17;
const num4 = 13;

// Create a variable called mod that calculates the remainder of dividing variable num3 by num4; the remainder should be 4
const mod = num3 % num4;

// Create an array of numbers
const numbers = [5, 10, 15, 20];

// Create a variable called max that finds the highest number in the set; the value returned should be 20
const max = Math.max(...numbers);

// Log the variables
console.log("Random number:", random);
console.log("Modulus:", mod);
console.log("Maximum number:", max);
